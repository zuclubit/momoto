name: momoto_flutter_example
description: "Momoto Flutter example — contrast validation, HCT palettes, CVD, glass materials"
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  # In production: add momoto_flutter as a path or pub.dev dependency
  # momoto_flutter:
  #   path: ../../packages/momoto-flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
